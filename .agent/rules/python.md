# Python Rules for Antigravity

- Follow PEP 8 style guidelines.
- Use explicit type hinting for function arguments and return values.
- Prefer `pathlib.Path` over `os.path` where applicable.
- Use virtual environments and structure modules cleanly.
- Handle exceptions explicitly with custom exception classes where appropriate.
