# React + Vite Rules for Antigravity

- Use Functional Components with React Hooks.
- Do not use class components.
- Keep state local where possible, and lift state up to Context (`useData.jsx`) for shared app entities.
- Fast Refresh compliance: Ensure components are exported cleanly without side effects in file root.
- Use explicit prop types or standard JS destructuring for props.
