# Go Rules for Antigravity

- Follow Effective Go idioms and standard `gofmt` code formatting.
- Always check errors explicitly (`if err != nil`).
- Keep interfaces small and define them where they are used.
- Use context (`context.Context`) for cancellation and timeouts in I/O operations.
- Prefer table-driven unit tests.
